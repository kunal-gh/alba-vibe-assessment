# Assignment 3 does not require secrets.
# The workflow uses public GitHub API endpoints and can run after import.

# Optional reference only. The importable workflow currently targets this repo:
GITHUB_REPOSITORY=kunal-gh/alba-vibe-assessment
